# Google Cloud Authentication for Testing
# Copy this file to .env.local and add your credentials

# Option 1: Service Account JSON File Path
# Download a service account key from Google Cloud Console
# GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\your\service-account-key.json

# Option 2: Service Account JSON Content (for CI/testing)
# GOOGLE_APPLICATION_CREDENTIALS_JSON={"type": "service_account", "project_id": "..."}

# Project Configuration
PROJECT_ID=bq_project_northstar
DATASET=demo_ai
LOCATION=US
BIGQUERY_REAL=1

# Usage:
# 1. Download service account key from Google Cloud Console
# 2. Set GOOGLE_APPLICATION_CREDENTIALS to the file path
# 3. Run: .\train-router.ps1 && .\build-neighbors.ps1
# 4. Test: python -m core.cli triage --router learned --graph-boost 0.2
