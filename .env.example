# Copy to .env and fill in your real values
# Example: cp .env.example .env

# =============================================================================
# GOOGLE CLOUD AUTHENTICATION
# =============================================================================
# Choose ONE of the following authentication methods:

# Method 1: API Key (Simplest - for testing only)
# Obtain from: Google Cloud Console > APIs & Credentials > API Keys
# GOOGLE_API_KEY=your_api_key_here

# Method 2: Service Account JSON (Recommended for production)
# Create at: Google Cloud Console > IAM & Admin > Service Accounts
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# Method 3: Default Application Credentials (For gcloud CLI users)
# Setup with: gcloud auth application-default login
# No environment variable needed - automatically detected

# =============================================================================
# BIGQUERY CONFIGURATION
# =============================================================================
BQ_PROJECT_ID=bq_project_northstar
BQ_LOCATION=US
BQ_DATASET=demo_ai

# Legacy service account option (deprecated - use GOOGLE_APPLICATION_CREDENTIALS instead)
# BQ_SERVICE_ACCOUNT_JSON=path_or_inline_json

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
# Switch to real BigQuery (default: stub mode)
# BIGQUERY_REAL=1

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. NEVER commit .env files to version control
# 2. API keys should be restricted to specific APIs and IPs when possible
# 3. Service account keys should have minimal required permissions
# 4. Rotate credentials regularly
# 5. Use Google Cloud IAM for fine-grained access control
